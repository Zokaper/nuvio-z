#include? "Version.xcconfig"

// Set NUVIO_IOS_TEAM_ID in CI or a developer-only xcconfig when signing is enabled.
// It is intentionally empty in source control so the project remains credential-neutral.
NUVIO_IOS_TEAM_ID=

PRODUCT_NAME=Nuvio Z
